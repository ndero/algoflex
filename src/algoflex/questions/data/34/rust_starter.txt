#[derive(Debug)]
struct TreeNode {
    val: i32,
    left: Option<Box<TreeNode>>,
    right: Option<Box<TreeNode>>,
}

fn are_cousins(root: &Option<Box<TreeNode>>, x: i32, y: i32) -> bool {
    // your move
}
