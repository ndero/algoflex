class TreeNode:
    __slots__ = ("left", "right", "val")

    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right


def are_cousins(root: TreeNode | None, x: int, y: int) -> bool:
    # your move
