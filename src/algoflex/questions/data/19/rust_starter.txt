#[derive(Debug)]
struct TreeNode {
    val: i32,
    left: Option<Box<TreeNode>>,
    right: Option<Box<TreeNode>>,
}

fn count_paths(arr: &Option<Box<TreeNode>>, target: i32) -> i32 {
    // your move
}